#include <bits/stdc++.h>
using namespace std;

int main(void) {
    int A = -1;
    int B = -1;
    cin >> A >> B;
    if(0 <= A <= 100 || 0 <= B <= 100) {
        cout << A + B << endl;
    }
}