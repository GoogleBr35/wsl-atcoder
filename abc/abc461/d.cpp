#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main(void) {
    // inputs
    ll h, w, k;
    cin >> h >> w >> k;
    vector<string> board(h);
    for(int i = 0; i < h; ++i) {
        cin >> board[i];
    }

    ll max = 0;
    ll count = 0;
    for(int i = 0; i < n; ++i) {
        if(i * i >= k)
    }
    
    // outputs
    cout << max << endl;

    return 0;
}